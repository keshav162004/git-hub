git init = inicilization of git

git status = check the status of git

git add filename = transfer file filesystem (untracked) to staged

git rm -cached filename =transfer filestaged to untracked (filesystem)

git commit -m " added filename " = transfer file staged to version control system (tracked)

git restore filename = restor the file 

git remote add origin https://github.com/keshav162004/k.git

git remote -v

git remote set-url origin https://"token key"@github.com/keshav162004/k.git

git push origin master = to paste the file into repostery

git clone  = Clone repostery to local

rm -rvf git-hub = to delete the file 
